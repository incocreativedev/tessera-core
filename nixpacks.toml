[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "python -m ensurepip --upgrade",
  "/opt/venv/bin/pip install --upgrade pip setuptools wheel",
  "/opt/venv/bin/pip install -e '.[railway]'"
]

[start]
cmd = "/opt/venv/bin/uvicorn tessera.api:app --host 0.0.0.0 --port $PORT"
