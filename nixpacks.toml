[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip setuptools wheel",
  "python -m pip install -e '.[railway]'"
]

[start]
cmd = "uvicorn tessera.api:app --host 0.0.0.0 --port $PORT"
