[build]
builder = "nixpacks"
buildCommand = "pip install -e '.[railway]'"

[deploy]
startCommand = "uvicorn tessera.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
